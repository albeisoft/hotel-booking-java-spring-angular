## MySQL, MariaDB configuration
spring.datasource.url = jdbc:mysql://localhost:3306/hotel_java?useSSL=false
spring.datasource.username = root
spring.datasource.password =

## JPA and Hibernate (ORM) configuration
# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = update
spring.jpa.hibernate.ddl-auto = create
spring.jpa.properties.hibernate.format_sql = true

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL10Dialect
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

## Logging
# logging.level.org.hibernate.SQL = debug
# logging.level.org.hibernate.type = trace
# logging.level.org.hibernate.SQL = info

# logging.file=C:/log/myapp.log

# logging.level.org.springframework.web = debug
# spring.http.log-request-details = true

## Server errors
# show errors from invalid validation from model annotation
server.error.include-binding-errors = always

## App properties
# app.jwtSecret = secretmysecret
# app.jwtExpirationInMs = 604800000